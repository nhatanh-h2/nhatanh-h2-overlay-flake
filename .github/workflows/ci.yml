name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-cache:
    uses: ./.github/workflows/build-and-cache.yml
    secrets: inherit
